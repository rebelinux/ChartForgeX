# ChartForgeX AD and Network Premium

Source: original hand-authored ChartForgeX topology SVG artwork.
Workflow: edit `svg/*.svg` as the source of truth, then refresh `previews/*.png` and validation reports with `ChartForgeX.Tools.IconImport --refresh-pack`.
License: ChartForgeX repository license, see `LICENSE` in this folder.

This pack keeps reviewable `manifest.json`, `svg/*.svg`, and generated `previews/*.png` side-by-side. Vendor and product names, when present, are nominative labels for diagramming scenarios. The artwork is intentionally original and does not copy official logos or claim vendor endorsement.
